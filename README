About
=====
HyperPublicJavaWrapper is a Java wrapper over the HyperPublic API that lets you query local places, deals, and events.

Usage
=====
1. Create Client Instance: HPClient client = HPClient.create(YOUR_CLIENT_ID,YOUR_SECRET);
2. Call API: List<Place> places = client.findPlaces("lat=40.7&lon=-74.0");


	
